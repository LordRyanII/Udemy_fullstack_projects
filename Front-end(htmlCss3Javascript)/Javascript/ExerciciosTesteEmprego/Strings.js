// Crie uma função que conte o número de caracteres em uma string.

const numeroString = (palavra) => {

    return console.log(`a palavra ${palavra}, contém esse número de palavras ${palavra.length}`)
}

numeroString('Ryan')
